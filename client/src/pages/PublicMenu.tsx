import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { MapPin, Search, UtensilsCrossed } from "lucide-react";
import { useMemo, useState } from "react";
import { useRoute } from "wouter";

const fallbackImages = ["/manus-storage/qrserve-menu-paneer_b1f548a1.jpg", "/manus-storage/qrserve-menu-tandoori_782fd2d0.jpg", "/manus-storage/qrserve-menu-biryani_8df97ed3.jpg"];

export default function PublicMenu() {
  const [, params] = useRoute("/menu/:slug"); const [search, setSearch] = useState(""); const [activeCategory, setActiveCategory] = useState("all"); const menu = trpc.public.menu.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug), retry: false });
  const categories = useMemo(() => (menu.data?.categories ?? []).filter(category => activeCategory === "all" || category.id === Number(activeCategory)).map(category => ({ ...category, items: category.items.filter(item => `${item.name} ${item.description ?? ""}`.toLowerCase().includes(search.toLowerCase())) })).filter(category => category.items.length > 0), [menu.data, activeCategory, search]);
  if (menu.isLoading) return <div className="min-h-screen bg-[#f6f2eb] px-5 py-12"><div className="mx-auto max-w-5xl animate-pulse"><div className="h-10 w-48 rounded bg-[#e6dfd6]" /><div className="mt-8 h-12 rounded bg-[#e6dfd6]" /><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{Array.from({ length: 6 }).map((_, index) => <div key={index} className="h-64 rounded-3xl bg-[#e6dfd6]" />)}</div></div></div>;
  if (menu.error || !menu.data) return <div className="grid min-h-screen place-items-center bg-[#f6f2eb] p-6 text-center"><div><UtensilsCrossed className="mx-auto h-8 w-8 text-[#a48e75]" /><h1 className="mt-5 font-display text-4xl text-[#201d19]">Menu not found.</h1><p className="mt-3 text-sm leading-6 text-[#70675d]">This menu may have moved or is no longer being served.</p></div></div>;
  const { restaurant } = menu.data;
  return <div className="min-h-screen bg-[#f6f2eb] text-[#201d19]"><header className="border-b border-[#e5ddd2] bg-[#fbf9f5]"><div className="mx-auto max-w-6xl px-5 py-10 sm:px-8"><p className="eyebrow text-[#8b7560]">Digital menu</p><h1 className="mt-3 font-display text-5xl tracking-[-0.05em] sm:text-6xl">{restaurant.name}</h1><div className="mt-4 flex flex-wrap items-center gap-3 text-sm text-[#756b60]"><span className="flex items-center gap-2"><MapPin className="h-4 w-4 text-[#ed5739]" />{restaurant.location}</span>{restaurant.description && <span className="hidden h-1 w-1 rounded-full bg-[#c9bdae] sm:inline" />}<span>{restaurant.description}</span></div></div></header><main className="mx-auto max-w-6xl px-5 py-8 sm:px-8"><div className="sticky top-0 z-20 -mx-5 border-b border-[#e5ddd2] bg-[#f6f2eb]/95 px-5 py-4 backdrop-blur sm:-mx-8 sm:px-8"><div className="flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-3 h-4 w-4 text-[#948a7e]" /><Input value={search} onChange={event => setSearch(event.target.value)} placeholder="Search the menu" className="h-10 rounded-xl border-[#dcd3c8] bg-white pl-10" /></div><div className="flex gap-2 overflow-x-auto pb-1">{[{id:"all",name:"All"}, ...menu.data.categories].map(category => <button key={category.id} onClick={() => setActiveCategory(String(category.id))} className={`shrink-0 rounded-full px-4 py-2 text-sm font-semibold transition ${activeCategory === String(category.id) ? "bg-[#201d19] text-white" : "border border-[#dcd3c8] bg-white text-[#61574c] hover:border-[#201d19]"}`}>{category.name}</button>)}</div></div></div>{categories.length === 0 ? <div className="grid min-h-72 place-items-center text-center"><div><Search className="mx-auto h-6 w-6 text-[#a48e75]" /><h2 className="mt-4 font-display text-3xl">No dishes matched that search.</h2><button onClick={() => { setSearch(""); setActiveCategory("all"); }} className="mt-3 text-sm font-semibold underline underline-offset-4">Clear filters</button></div></div> : <div className="pt-8">{categories.map(category => <section key={category.id} className="mb-12 last:mb-0"><div className="mb-5 flex items-end justify-between border-b border-[#ded5ca] pb-4"><div><p className="eyebrow text-[#8b7560]">Menu</p><h2 className="mt-2 font-display text-4xl">{category.name}</h2></div>{category.description && <p className="hidden max-w-xs text-right text-sm leading-5 text-[#786d61] sm:block">{category.description}</p>}</div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{category.items.map((item, index) => <article key={item.id} className="overflow-hidden rounded-3xl border border-[#e2dad0] bg-[#fbf9f5] shadow-[0_8px_25px_rgba(57,42,26,0.04)]"><img src={item.imageUrl || fallbackImages[index % fallbackImages.length]} alt={item.name} className="h-44 w-full object-cover" /><div className="p-5"><div className="flex items-start justify-between gap-4"><h3 className="font-display text-2xl leading-none">{item.name}</h3><span className="shrink-0 font-display text-xl">₹{item.price.toFixed(2).replace(/\.00$/, "")}</span></div>{item.description && <p className="mt-3 text-sm leading-6 text-[#746b60]">{item.description}</p>}</div></article>)}</div></section>)}</div>}</main><footer className="mt-12 border-t border-[#e5ddd2] px-5 py-8 text-center text-xs text-[#8b8176]">Powered by QRServe</footer></div>;
}
